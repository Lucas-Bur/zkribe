# zkribe

KI-Transkription mit Vite+, TanStack Start und TanStack AI über OpenRouter.

```sh
cp .env.example .env
vp install
vp dev
```

Vite+ verwendet standardmäßig `http://localhost:5173` und weicht bei einem
belegten Port automatisch aus. Benötigt wird nur `OPENROUTER_API_KEY`.
